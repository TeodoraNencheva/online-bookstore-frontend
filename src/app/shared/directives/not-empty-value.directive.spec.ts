import { NotEmptyValueDirective } from './not-empty-value.directive';

describe('NotEmptyValueDirective', () => {
  it('should create an instance', () => {
    const directive = new NotEmptyValueDirective();
    expect(directive).toBeTruthy();
  });
});
